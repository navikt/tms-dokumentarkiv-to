---
import {BodyLong, Link} from "@navikt/ds-react";
import styles from "./PageHeader.module.css";

interface BreadcrumbItem {
  label: string;
  href: string;
}

interface Props {
  description?: string;
  breadcrumbItems: BreadcrumbItem[];
}

const {description, breadcrumbItems} = Astro.props;
---

<div class={styles.pageHeader}>
  <nav aria-label="Brødsmuler" class={styles.breadcrumb}>
    <ol class={styles.breadcrumbList}>
      {
        breadcrumbItems.map(({href, label}) => (
          <li class={styles.breadcrumbItem}>
            <Link href={href}>{label}</Link>
          </li>
        ))
      }
    </ol>
  </nav>
  <slot />
  {
    description && (
      <BodyLong className={styles.description} size="large">
        {description}
      </BodyLong>
    )
  }
</div>
