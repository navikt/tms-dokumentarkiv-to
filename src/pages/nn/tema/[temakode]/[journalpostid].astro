---
import SingleJournalpostContainer from "@components/containers/SingleJournalpostContainer";
import { text } from "@language/text";
import Layout from "@layouts/Layout.astro";
import { baseUrl } from "@src/urls";

const { temakode, journalpostid } = Astro.params;
const breadcrumbs = [
  {
    title: text.minSide["nn"], url: "/minside",
  },
  {
    url: baseUrl,
    title: text.dokumentarkiv["nn"],
  },
  {
    url: `${baseUrl}/tema/${temakode}`,
    title: text.dokumenter["nn"],
  },
];
---

<Layout title="Dokumentarkiv" breadcrumbs={breadcrumbs} language="nn">
  <SingleJournalpostContainer language="nn" client:only="react" journalpostId={journalpostid}/>
</Layout>
