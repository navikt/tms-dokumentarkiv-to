---
import SingleJournalpostContainer from "@components/containers/SingleJournalpostContainer";
import { text } from "@language/text";
import Layout from "@layouts/Layout.astro";
import { baseUrl } from "@src/urls";

const { temakode, journalpostid } = Astro.params;
const breadcrumbs = [
  {
    title: text.minSide["en"], url: "/minside",
  },
  {
    url: baseUrl,
    title: text.dokumentarkiv["en"],
  },
  {
    url: `${baseUrl}/tema/${temakode}`,
    title: text.dokumenter["en"],
  },
];
---

<Layout title="Dokumentarkiv" breadcrumbs={breadcrumbs} language="en">
  <SingleJournalpostContainer language="en" client:only="react" journalpostId={journalpostid}/>
</Layout>
