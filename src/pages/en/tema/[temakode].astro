---
import DokumentlisteContainer from "@components/containers/DokumentlisteContainer";
import { text } from "@language/text";
import Layout from "@layouts/Layout.astro";
import { baseUrl } from "@src/urls";

const { temakode } = Astro.params;
const breadcrumbs = [
  {
    title: text.minSide["en"], url: "/minside",
  },
  {
    url: baseUrl,
    title: text.dokumentarkiv["en"],
  },
  {
    url: `${baseUrl}/tema/${temakode}`,
    title: text.dokumenter["en"],
  },
];
---

<Layout title="Dokumentarkiv" breadcrumbs={breadcrumbs} language="en">
  <DokumentlisteContainer language="en" client:only="react" temakode={temakode}/>
</Layout>
